<Window x:Class="demoXUETA.Views.LoginView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:demoXUETA.Views"
        mc:Ignorable="d"
        Title="LoginView" Height="450" Width="350" MinHeight="450" MinWidth="350" WindowStartupLocation="CenterScreen">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="2*"/>
            <RowDefinition Height="8*"/>
            <RowDefinition Height="2*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="1" HorizontalAlignment="Center" VerticalAlignment="Center">
            <Label Content="Авторизация"/>
            <Label Content="Логин" Margin="0 10 0 0"/>
            <TextBox x:Name="loginBox"/>
            <Label Content="Пароль"/>
            <PasswordBox x:Name="passBox"/>

            <Button x:Name="loginButton" Content="Войти" Margin="0 10 0 0" Click="loginButton_Click"/>

        </StackPanel>

    </Grid>
</Window>
